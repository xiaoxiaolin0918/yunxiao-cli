# Shell completion

```bash
yunxiao completion bash
yunxiao completion zsh
yunxiao completion powershell
```

Windows：当前会话 `yunxiao completion powershell | Out-String | Invoke-Expression`；写入 `$PROFILE` 可持久化。

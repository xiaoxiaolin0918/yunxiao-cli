# 0.16.11

## 变更

- **fix(pipeline):** `+pending --all-pipelines` 中途 403/5xx 改为部分成功；`meta.scanned` / `skipped_no_permission` / `errors`（#29）。
- **fix(pipeline):** `update --validate` 仅在结构 diff 空且全文 YAML+name 均一致时跳过 PUT（`mode=validate_noop`）；flag 文案写明仍会写；新增 `--check` = `--validate --dry-run`（#30）。

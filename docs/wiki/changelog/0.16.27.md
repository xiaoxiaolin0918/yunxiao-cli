# 0.16.27

- **fix(workitem/#82):** `+transition --dry-run` option A — when verified `workflows[].edges` 为空但 `hinted_edges` 非空，仍按 hinted 分类（`hinted` / `illegal`→`ok:false`）；仅当 **edges 与 hinted_edges 皆空**（或 `transition_mode=direct_status`）时才 `edge_validation=skipped`。`ResolveWorkflow` 在仅有 `hinted_edges` 时亦返回 profile 图（不再因 verified edges 为空而落到 direct_status）。
- **docs(termui/#81):** 复现确认 0.16.26 起 CLI 自身不向 stdout/stderr/`script` 捕获写入 ESC[H ESC[2J ESC[3J]（无 TUI 依赖）；`Configure()` + `YUNXIAO_NO_TUI`/`NO_COLOR`/`TERM=dumb` 足够；`WriteTTY`/`StripClearScreen` 保留给未来 TUI 路径，无需额外接线。

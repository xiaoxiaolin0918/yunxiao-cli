# 0.16.26

- **fix(termui/#76):** 非 TTY / `TERM=dumb` / `NO_COLOR` / `YUNXIAO_NO_TUI=1` 时禁用清屏与花哨终端控制；PersistentPreRun 强制 `NO_COLOR`，避免 agent 捕获被 ESC[H ESC[2J ESC[3J] 污染。
- **fix(workitem/#75):** `+transition --dry-run`：`workflows[].edges` **为空**时仍 `edge_validation=skipped`（即使仅有 `hinted_edges`）；有非空 verified edges 后才按 edges∪hinted_edges 分类——实证边 → `validated`；仅 hinted → `hinted`+warning；两边皆无 → `ok:false`（含源不在边表的误放行）。`MergeWorkflow`/`--write-profile` 对 edges 为并集。
- **feat(workitem/#77):** `+bug-create --verifier <userId|self>`；缺省取 `profile.default_verifier` 或 `workitem_defaults` verifier；未设置时 stderr 告警；成功回显 `verifier`。`--serious-level` help 与映射对齐，并接受 serious↔severe、minor↔slight 同义。

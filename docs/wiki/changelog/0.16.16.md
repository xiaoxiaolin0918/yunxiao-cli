# 0.16.16

- **fix(client):** document GET transport retries; classify transient network errors (#47)
- **fix(client/cmd):** on write network failure, error includes \check before retry:\ with ready search command (mrs create/+create → \mrs list --search\; +bug-create → \workitem search --subject\) (#47)
- Long-term \--dedupe-key\ not in this release

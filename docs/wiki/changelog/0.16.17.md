# 0.16.17

- **feat(codeup):** `yunxiao codeup mrs link` / `mrs unlink` — 通过 Projex `extRelationRecords`（category=`codeupMergeRequest`）给**已有** MR 挂/解工作项；幂等；支持 `--dry-run`（#54）
- **feat(codeup):** `mrs update --work-item` 可单独或与 `--title`/`--description` 组合；挂单语义与 `mrs link` 相同（加法，不用 UpdateChangeRequest）
- **fix(mrlink):** 挂单失败提示改为建议 `yunxiao codeup mrs link ...`

# 0.16.7 — pipeline 人工卡点闭环 (#22)

## Plan
关闭 ManualValidate CLI 环：发现待审卡点 → 通过/拒绝 → 等待终态/卡点暂停。

## Do
- `internal/pipelinegate`：从 run detail 抽取 pending jobs；`ClassifyWatchOutcome` 退出码 0/1/2/3/-1
- `yunxiao pipeline +pending`：扫描 WAITING（可选 RUNNING）运行并输出可复制 pass/refuse 命令
- `yunxiao pipeline run watch`：轮询至终态或 gate_paused（exit 3）；超时 exit 4
- `job pass|refuse` 继续走 `--yes`/`-y`；快捷命令 `+approve` / `+refuse`

## Check
- `go test ./internal/pipelinegate/ ./cmd/`
- `go build .`
- 无活体 WAITING 运行时未做联调（组织内当时无 WAITING）

## Act
后续可用真实卡点跑一次 `+pending` → `+approve --yes` → `run watch` 冒烟。

# 0.16.13

- **feat(browse):** `yunxiao browse` 打开 Flow/Projex/Codeup 控制台（`pipeline` / `workitem` / `mr` / `repo` / `url`）；`--print-only` 只打印 URL。
- **docs:** README 人类 30 秒快速开始；去掉安装示例中的钉死旧版本；usage 索引与 gh/`yx` 迁移映射（#34 #35 #36 #37）。

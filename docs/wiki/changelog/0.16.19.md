# 0.16.19

- **fix(workitem):** `workitem create` 成功输出默认 brief（保留 `id` / `serialNumber` / `status.displayName` / `subject`，去掉 description 等噪声）；当 create API 返回 `serialNumber`/`status` 为 null 时自动再 GET 补齐（#62）。`--full` 输出完整对象。
